# Pruebas End-to-End

Esta carpeta queda preparada para recorridos con Playwright cuando el frontend y el backend se ejecuten en un ambiente de pruebas.

Después de restaurar paquetes, instale los navegadores de Playwright con el script generado dentro de `bin/Debug/net8.0`.
